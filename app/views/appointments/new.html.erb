  <div class="row justify-content-center form-container">
    <div class="col-12 col-sm-4">
      <div class="greeting">
        <%= "Nous sommes le #{Date.today.strftime("%A %e %B %Y")}." %>
        </div>
      </div>
      <div>
        <h2>Ajouter un rendez-vous</h2>
        <%= render "form", appointment: @appointment %>
      </div>
